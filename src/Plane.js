const Airport = require("./Airport.js");

class Plane {
    // plane id attribute
    id;

    // undefined value for id
    constructor(id) { this.id = id; }

    // get plane id
    // getPlaneId() {return this.id;}
}

module.exports = Plane;